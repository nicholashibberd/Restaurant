<% if signed_in? %>
	Signed in as: <%= current_user.status.titlecase %>: 
	<span id="sign_in_out_link"><%= link_to "Sign out", signout_path, :method => :delete %></span>
<% else %>
		<span id="sign_in_out_link"><%= link_to "Sign in", signin_path %></span>
<% end %>
